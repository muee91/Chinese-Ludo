name: Apply Six Player UI Polish

on:
  push:
    branches:
      - agent/add-six-player-foundation
    paths:
      - scripts/fix-six-player-ui-polish.mjs
      - .github/workflows/apply-six-player-ui-polish.yml

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/add-six-player-foundation
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/fix-six-player-ui-polish.mjs
      - name: Commit fixups
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix: polish six-player integration across original UI"
          git push origin HEAD:agent/add-six-player-foundation
